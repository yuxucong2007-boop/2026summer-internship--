我先列一下 `kmind/` 目录的实际文件，再给你说明每段代码的作用：
所有代码都在 **`C:\Users\Yu\WorkBuddy\2026-08-19-09-53-59\kmind\`** 目录下。按用途分三组：

## 📜 核心代码（3 个脚本，按执行顺序）

| 文件                  | 大小     | 作用                                                                                                                                                                           |
| ------------------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **`eval.py`**       | 5.2 KB | **主评测脚本**：读取测试集 114 题，用 `ThreadPoolExecutor` 并发 5 路调用 `curl -sk`（因服务器要求 TLS 重协商，Python SSL 握手会失败，故走 curl 子进程），逐行解析流式响应提取 `sources` 检索块，结果写入 `results_raw.jsonl`              |
| **`report_gen.py`** | 11 KB  | **报告生成**：汇总所有指标、按文档拆分统计、整理 6 类典型案例，生成 `report.md` + `details.csv`                                                                                                            |
| **`analyze.py`**    | 6.4 KB | **指标计算**：把检索块映射到具体页码（三层策略：元数据 `page_label` → 页头页码模式 `福建靠谱云算力技术有限公司\n(页码)\n` → 本地 PDF 逐页签名比对），再与测试集的 `relevant_pages` 求交集，算出 Hit@k / Recall@k / 干扰题指标，输出 `analysis_rows.json` |

## 🧪 辅助脚本与调试文件

- **`probe.py`**（1.6 KB）——开发阶段用的单题探测脚本，验证流式响应解析、知识库 collection 附加是否生效
- **`token.txt`** ——你的 JWT 访问令牌（每次调用都从这读取）
- **`files.json`** ——知识库 collection 信息（`KID` = `5e4a239c-...`，即附加的 knowledge_id）
- **`pdf_pages.json`** ——4 个 PDF 共 53 页的本地逐页文本，供页码签名比对用
- **`req_stream.json` / `req_test.json` / `resp_stream.txt` / `resp_test.json`** ——探测阶段的请求/响应样例
- **`chat_ids.json` / `test_chat_ids.txt` / `chats*.json`** ——120 个测试会话的 ID 与列表（清理时用）

## 📄 最终产物

- `report.md`（完整测试报告）、`details.csv`（逐题明细）、`metrics_guide.md`（指标解读文档）

**一句话总结调用链**：`eval.py` 跑题 → `results_raw.jsonl` → `analyze.py` 算指标 → `analysis_rows.json` → `report_gen.py` 出报告。其中 `eval.py` 是最耗时的环节（114 题 × 并发 5，约 20 分钟）。

如果需要我讲解某个脚本的实现细节，或想改造成可复用的评测工具（换测试集/换知识库重跑），随时说。